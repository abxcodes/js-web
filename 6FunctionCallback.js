//callback function

function thirdumpaireRev(groundumpreq,callbackf){

    if(groundumpreq) return
    groundumpreq && callbackf()
}

function checkCamangle(){
   // umpare is checking some visuals
   //umpare has a final decision now
   const des1=parseInt(prompt("what is decision?")) 

   console.log("out hehehe");
}

thirdumpaireRev(true,checkCamangle)